name: Deploy Website

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate HTML
        uses: html-validator/html-validator-action@v1
        with:
          file: "index.html"

      - name: Deploy
        run: echo "Deploy done via GitHub Pages"
