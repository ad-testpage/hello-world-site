name: Deploy Website

# Sproži workflow ob push-u na master (ali main)
on:
  push:
    branches:
      - master  # ali main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repozitorija
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Deploy korak (osnovni primer)
      - name: Deploy
        run: echo "Deploy done 🚀"
